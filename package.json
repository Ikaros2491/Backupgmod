{
  "name": "vr-prototype",
  "version": "0.1.0",
  "private": true,
  "description": "WebXR VR prototype playground — immersive scene with controllers, grab, and teleport",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "three": "^0.178.0"
  },
  "devDependencies": {
    "@types/three": "^0.178.0",
    "typescript": "^5.8.3",
    "vite": "^7.0.4"
  },
  "engines": {
    "node": ">=20"
  }
}
